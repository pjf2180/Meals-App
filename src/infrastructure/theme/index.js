import { colors } from "./colors";
import { space, lineHeights, spaceSizes } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  spaceSizes,
  fonts,
  fontSizes,
  fontWeights,
};
